@IsTest
public with sharing class CreateNewClassTest {
    @IsTest
    static void testCreateNewClass() {
        Test.startTest();
        CreateNewClass.testing();//test2
        Test.stopTest();
    }
}